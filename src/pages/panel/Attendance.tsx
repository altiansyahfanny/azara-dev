import Container from '@/components/core/container';
import useTitle from '@/hooks/useTitle';

const Attendance = () => {
	useTitle('Kehadiran');

	return <Container title="Kehadiran">Attendance</Container>;
};

export default Attendance;
